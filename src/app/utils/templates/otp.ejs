<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>OTP Verification</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f5f5f5;
        margin: 0;
        padding: 20px;
      }
      .container {
        max-width: 600px;
        margin: 0 auto;
        background-color: #ffffff;
        padding: 40px;
        border-radius: 8px;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
      }
      h2 {
        color: #333333;
        margin-top: 0;
      }
      p {
        color: #666666;
        line-height: 1.6;
        margin: 15px 0;
      }
      .otp-code {
        background-color: #f0f0f0;
        padding: 15px;
        border-radius: 4px;
        text-align: center;
        font-size: 24px;
        font-weight: bold;
        letter-spacing: 2px;
        color: #333333;
        margin: 20px 0;
      }
      .validity {
        color: #ff6b6b;
        font-size: 14px;
        margin-top: 20px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h2>Hello <%= name %>,</h2>
      <p>Your OTP code is:</p>
      <div class="otp-code"><%= otp %></div>
      <p class="validity">This code is valid for 2 minutes.</p>
    </div>
  </body>
</html>
